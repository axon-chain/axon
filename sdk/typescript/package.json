{
  "name": "@axon-chain/sdk",
  "version": "0.3.0",
  "description": "TypeScript SDK for the Axon AI Agent blockchain",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["axon", "blockchain", "ai-agent", "sdk", "ethers"],
  "author": "Axon Chain",
  "license": "Apache-2.0",
  "dependencies": {
    "ethers": "^6.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "files": ["dist"],
  "engines": {
    "node": ">=16"
  }
}
